import React, { Fragment } from "react";
import { DataContent as DataContentModule } from "../../contentComponents/dataContent/dataContent";

const DataContent = () => {
  return (
    <Fragment>
      <DataContentModule />
    </Fragment>
  );
};

export { DataContent };
